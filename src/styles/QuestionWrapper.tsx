import styled from "styled-components";

const QuestionWrapper = styled.div`
  padding: 20px 0;
  p:last-of-type {
    margin-bottom: 20px; /* 마지막 p 태그의 margin-bottom 적용 */
  }
`;

export default QuestionWrapper;
